# Renderer Next

Destino futuro do renderer modular. A migração visual ocorre depois dos domínios e do shell Electron atingirem paridade.

Enquanto isso, `src/renderer/` continua sendo o único renderer da Lumi distribuída.
